import styled from 'styled-components';
import { COLOR_GREY_3 } from 'Styles/colors';

const NotFoundPageMessage = styled.div`
  color: ${COLOR_GREY_3};
  font-size: 3rem;
  font-weight: 700;
  margin-right: 2rem;
  text-transform: uppercase;
`;

export default NotFoundPageMessage;
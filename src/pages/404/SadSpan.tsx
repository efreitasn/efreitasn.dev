import styled from 'styled-components';
import { COLOR_GREY_2 } from 'Styles/colors';

const NotFoundPageSadSpan = styled.span`
  color: ${COLOR_GREY_2};
  font-size: 4rem;
  font-weight: bold;
  margin-top: -1rem;
`;

export default NotFoundPageSadSpan;
import styled from 'styled-components';

const PageTitle = styled.h1`
  font-size: 2.8rem;
  line-height: 3.6rem;
  font-weight: 700;
  padding: 0 4rem;
  margin-top: 2rem;
  margin-bottom: 1rem;
`;

export default PageTitle;